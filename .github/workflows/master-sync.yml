name: Sync to Public Repo

on:
  push:
    branches:
      - krishna-changes

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for a complete mirror

      - name: Push to public repo
        run: |
          git remote add public https://x-access-token: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@github.com/KrishnaMehta123/TestPublicIntegration.git
          git push public main --force
